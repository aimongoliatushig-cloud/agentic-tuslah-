# Suspicious API Key

1. Locate the client by API key preview or usage logs.
2. Regenerate the key from the admin UI.
3. Confirm old `api_keys` rows are `revoked`.
4. Review `api_usage_logs` for unusual spend or providers.
5. Reduce budget limits while investigating.
